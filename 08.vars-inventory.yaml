- name: variables from inventory
  hosts: local
  connection: local
  tasks:
  - name: Printing varaibles
    ansible.builtin.debug:
      msg: " course name is {{ Course }}, duration is : {{ Duration }}, thrainer is: {{ Trainer }}"
